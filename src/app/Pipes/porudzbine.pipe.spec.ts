import { PorudzbinePipe } from './porudzbine.pipe';

describe('PorudzbinePipe', () => {
  it('create an instance', () => {
    const pipe = new PorudzbinePipe();
    expect(pipe).toBeTruthy();
  });
});
